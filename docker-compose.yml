# Stack de almacenamiento y visualización para la monitorización del CPD.
#
# El colector Go NO está en este docker-compose a propósito: necesita
# acceso directo al adaptador Bluetooth del host a través de BlueZ/D-Bus,
# y meterlo en un contenedor obliga a montar sockets del sistema y usar
# red en modo "host", lo que complica el proyecto sin aportar nada en un
# primer despliegue. El colector se ejecuta como servicio systemd nativo
# en el host (ver deploy/systemd/cpd-monitor.service y el README).
# Contenerizarlo más adelante está recogido como mejora futura en
# docs/MEJORAS-FUTURAS.md.

services:
  influxdb:
    image: influxdb:2.7
    container_name: cpd-influxdb
    restart: unless-stopped
    ports:
      - "8086:8086"
    volumes:
      - influxdb-data:/var/lib/influxdb2
      - influxdb-config:/etc/influxdb2
    environment:
      DOCKER_INFLUXDB_INIT_MODE: setup
      DOCKER_INFLUXDB_INIT_USERNAME: ${INFLUXDB_ADMIN_USER}
      DOCKER_INFLUXDB_INIT_PASSWORD: ${INFLUXDB_ADMIN_PASSWORD}
      DOCKER_INFLUXDB_INIT_ORG: ${INFLUXDB_ORG}
      DOCKER_INFLUXDB_INIT_BUCKET: ${INFLUXDB_BUCKET}
      DOCKER_INFLUXDB_INIT_ADMIN_TOKEN: ${INFLUXDB_ADMIN_TOKEN}

  grafana:
    # Grafana OSS 13.x es la línea estable en el momento de escribir esto.
    # Comprueba en https://hub.docker.com/r/grafana/grafana-oss/tags si hay
    # una versión más reciente antes de desplegar en producción, y fija
    # siempre una versión concreta (nunca "latest") para evitar sorpresas.
    image: grafana/grafana:13.0.2
    container_name: cpd-grafana
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD}
      # Necesario para que el datasource provisionado pueda usar la
      # variable ${INFLUXDB_ADMIN_TOKEN} (ver grafana/provisioning/datasources).
      INFLUXDB_ADMIN_TOKEN: ${INFLUXDB_ADMIN_TOKEN}
      INFLUXDB_ORG: ${INFLUXDB_ORG}
      INFLUXDB_BUCKET: ${INFLUXDB_BUCKET}
    depends_on:
      - influxdb

volumes:
  influxdb-data:
  influxdb-config:
  grafana-data:
